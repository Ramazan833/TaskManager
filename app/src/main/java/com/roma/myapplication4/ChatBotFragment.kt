package com.roma.myapplication4

import androidx.fragment.app.Fragment

/**
 * This fragment is no longer in use. Its content has been removed to resolve
 * build errors after the associated API key and libraries were deleted.
 */
class ChatBotFragment : Fragment() {
    // The functionality has been removed.
}
